"""
Tests for moderation, as of now this is unused because moderation
already gets good test coverage in other test cases (since it is
used everywhere).
"""

# from django.test import TestCase

# Create your tests here.
